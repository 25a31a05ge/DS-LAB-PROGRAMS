
#include <iostream>
using namespace std;
int main() {
    int data[5]; 
    int* ptr;   
    ptr = data; 
    cout << "Enter 5 elements:" << endl;
    for (int i = 0; i < 5; ++i) {
        cin >> *(ptr + i); 
    }
    cout << "You entered:" << endl;
    for (int i = 0; i < 5; ++i) {
        cout << *(data + i) << endl;
    }
    return 0;
}
